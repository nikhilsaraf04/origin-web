import { PlaceFormScreen } from "@/components/screens/PlaceFormScreen";

export const metadata = { title: "New visit — Origin" };

export default function NewPlacePage() {
  return <PlaceFormScreen />;
}
